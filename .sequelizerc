require("@babel/register");

const path = require("path");

module.exports = {
	config: path.resolve("answer-2/config", "sequelize.cjs"),
	"models-path": path.resolve("answer-2/infrastructure/database", "models"),
	"seeders-path": path.resolve("answer-2/infrastructure/database", "seeders"),
	"migrations-path": path.resolve(
		"answer-2/infrastructure/database",
		"migrations"
	),
	debug: true,
};
